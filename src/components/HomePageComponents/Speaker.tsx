import React from "react";
import MaxContainer from "../MaxContainer";

const Speaker = () => {
    return (
        <MaxContainer className="">
            <div className="font-bold text-3xl h-[150vh] bg-orange-300 flex items-center justify-center" id="ticket">Speaker</div>
        </MaxContainer>
    );
};

export default Speaker;
