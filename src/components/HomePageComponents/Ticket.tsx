import React from "react";
import MaxContainer from "../MaxContainer";

const Ticket = () => {
    return (
        <MaxContainer className="">
            <div className="font-bold text-3xl h-[150vh] bg-red-300 flex items-center justify-center" id="speaker">Ticket</div>
        </MaxContainer>
    );
};

export default Ticket;
