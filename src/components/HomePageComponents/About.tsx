import React from "react";
import MaxContainer from "../MaxContainer";

const About = () => {
    return (
        <MaxContainer className="mt-16">
            <div className="font-bold text-3xl h-[150vh] bg-teal-300 flex items-center justify-center" id="about">About</div>
        </MaxContainer>
    );
};

export default About;
